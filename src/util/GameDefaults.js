export const initGrade = (length) => {
  return {
    0: Array.from({ length }).fill(''),
    1: Array.from({ length }).fill(''),
    2: Array.from({ length }).fill(''),
    3: Array.from({ length }).fill(''),
    4: Array.from({ length }).fill(''),
    5: Array.from({ length }).fill(''),
    6: Array.from({ length }).fill(''),
    7: Array.from({ length }).fill(''),
    8: Array.from({ length }).fill(''),
    9: Array.from({ length }).fill(''),
  }
}